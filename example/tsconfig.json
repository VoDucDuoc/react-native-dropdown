{
  "extends": "../tsconfig",
  "compilerOptions": {
    // Avoid expo-cli auto-generating a tsconfig
    "baseUrl": ".",
    "paths": {
      "@duocvo/react-native-dropdown": ["../src/index"]
    }
  }
}
